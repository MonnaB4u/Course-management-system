import React from 'react';
import Header from './Header/Header';

const componentName = () => {
    return (
        <div>
            <Header></Header>
        </div>
    );
};

export default componentName;